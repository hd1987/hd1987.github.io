language: node_js
node_js:
  - 10

# S: Build Lifecycle
install:
  - npm install
  - npm install hexo-generator-searchdb --save

#before_script:

script:
  - hexo clean && hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "AdiHuang"
  - git config user.email "huangdi1987@163.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - dev

env:
  global:
    - GH_REF: github.com/hd1987/hd1987.github.io.git
